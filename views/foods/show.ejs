<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= food.name %> Details</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= food.name %> Details</h1>
    <p>Item Name:<%= food.name %></p>
    <p>Item Category:<%= food.category %></p>
    <p>Item Quantity:<%= food.quantity %></p>
    <p>Item Notes:<%= food.notes %></p>
  </body>
</html>
